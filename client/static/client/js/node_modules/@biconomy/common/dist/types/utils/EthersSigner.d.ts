import { SmartAccountSigner } from "@alchemy/aa-core";
import { Hex } from "viem";
import { Signer } from "@ethersproject/abstract-signer";
export declare class EthersSigner<T extends Signer> implements SmartAccountSigner<T> {
    #private;
    signerType: string;
    inner: T;
    constructor(inner: T, signerType: string);
    getAddress(): Promise<`0x${string}`>;
    signMessage(message: string | Uint8Array): Promise<Hex>;
    signTypedData(_notUsed: any): Promise<Hex>;
}
export default EthersSigner;
//# sourceMappingURL=EthersSigner.d.ts.map