import { isBaseSmartAccountClient } from "../../client/isSmartAccountClient.js";
import { IncompatibleClientError } from "../../errors/client.js";
import { buildUserOperation } from "./buildUserOperation.js";
export const checkGasSponsorshipEligibility = async (client, args) => {
    if (!isBaseSmartAccountClient(client)) {
        throw new IncompatibleClientError("BaseSmartAccountClient", "checkGasSponsorshipEligibility");
    }
    return buildUserOperation(client, args)
        .then((userOperationStruct) => userOperationStruct.paymasterAndData !== "0x" &&
        userOperationStruct.paymasterAndData !== null)
        .catch(() => false);
};
//# sourceMappingURL=checkGasSponsorshipEligibility.js.map